# Anchor captures v1

Capture date: 2026-09-02

Viewport: 1280 × 800, deviceScaleFactor 2.

These captures freeze against DESIGN.md commit `2fa759031244b758e9e0fa094159d5657727424d` (`Re-designate anchor 2: demo replaces nonexistent case study page`).

Re-anchoring is an explicit versioned act; see signature-anchors clause.

## timeline-v1

- URL: https://www.royamontgomery.com/
- Path: `/`
- Portfolio site git commit: `c1f56b6ebb2ce84ccf157ceae603f83de3a43376` (`Metadata in text face; mono narrowed to figure annotation and code per contract`)
- Files: `full.png` plus `section-01.png` … `section-23.png` (page height 18164px)

## demo-v1

- URL: https://loop-visibility.vercel.app/run
- Path used: `/run`. That route is the hop-trace UI (label “HOP TRACE”, dashed trace well). `/` is the fleet overview, not the trace surface.
- The demo’s repo lives separately from this contract and from the portfolio site.
- Recaptured 2026-09-02 by starting a money check-in on `/run`. Captures include a mid-run frame, the completed trace with hops collapsed, and one expanded-evidence frame.
- State choice: the primary completed freeze is the collapsed hop list — the surface’s default read. A fully expanded capture buried that composition under many screenfuls of raw INPUT/OUTPUT JSON and would hand the judge a monospace payload instead of the product UI. One extra frame expands only VERIFY, so the evidence-beside-the-verdict claim is in the set without dominating it.
- In-progress (`running-*`): status Running…, run `9a2f1c5a`, realtime, 1 hop; PLAN row plus “Agent is gathering transactions…”. Page height 800px. Files: `running-full.png`, `running-section-01.png`.
- Completed, collapsed (`full.png`, `section-*`): same run `9a2f1c5a`, 5 hops (PLAN, GATHER, GATHER, ACT, VERIFY), all rows collapsed. Page height 800px. Files: `full.png`, `section-01.png`.
- Expanded evidence (`expanded-verify*`): same run, only VERIFY open (evidence cards: AWS Reversed; Netflix / NYT / YouTube Premium Confirmed). Raw evidence left closed. Page height 1292px. Files: `expanded-verify.png` (viewport, VERIFY in frame), `expanded-verify-full.png` (full page).
